# C-Prog0

# About Our Software
This file contains 6 programs created in Bash.

prog1.c prompts a user for an interger in the cmd line, then accepts n strings from standard input

prog2.c includes C functions such as my_openit() and my_closeit() that matches the types of the function pointers in the structure

prog3.c prints the elements of an array

prog4.c outs the individual bytes of an unsigned long value

prog5.c accepts a single command line arguement of a file to be processed, tells the user the amount of bytes in the file and outputs the contents in the file

small.c prints an X pattern. 

## Commands
gcc -o [file name] [file name.c]
